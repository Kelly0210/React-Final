
let initialState = {
    bestFriends: [
        {id: 1, name: "Dimych"},
        {id: 2, name: "Andrey"},
        {id: 3, name: "Sveta"},
    ],
};

const SidebarPageReducer = (state = initialState, action) => {

    return state;
};

export default SidebarPageReducer;